# 00 — 总览与优化优先级

> **状态**：设计方案，未实现；基线 `b35d1a2`；当前事实 = 静态代码证据，指标 = 拟定验收目标

---

## 项目独特价值

z-biz-tool-file 是**三合一桌面文件管理器**：

1. **深度预览**：图片编辑（18+ 调整项）、视频/音频播放、PDF/epub/Office 文档、Markdown、CSV/JSON/YAML 结构化展示 — 预览格式覆盖远超普通文件管理器
2. **开发者工具集成**：内置终端、Git 状态、Diff 对比、哈希计算、目录同步、压缩包浏览/创建 — 替代多个独立工具
3. **AI 辅助**：LLM 摘要（OpenAI/Anthropic/Ollama）、规划中的智能分类/语义搜索 — 差异化竞争力

技术栈：Tauri 2 (Rust) + React 19 + TypeScript 5 + Ant Design 6 + Zustand 5 + Vite 6

---

## 文档索引

| # | 文档 | 核心内容 |
|---|------|----------|
| 01 | [现状审计与问题清单](./01_现状审计与问题清单.md) | 15 项已确认缺陷 + 5 项待验证假设 + 14 项已存在能力 |
| 02 | [产品能力与交互优化](./02_产品能力与交互优化.md) | 9 大用户流程缺口分析 + 12 项交互改造排序 |
| 03 | [技术架构与性能优化](./03_技术架构与性能优化.md) | 8 项明确推荐方案 + 数据模型 + IPC 契约 |
| 04 | [数据安全与可靠性](./04_数据安全与可靠性.md) | 路径防护设计 + 权限收敛 + Agent 安全边界 |
| 05 | [实施路线与任务拆解](./05_实施路线与任务拆解.md) | 5 阶段 40+ 任务 + 依赖图 + 回滚策略 |
| 06 | [测试验收与风险清单](./06_测试验收与风险清单.md) | 验收矩阵（单元/集成/E2E/安全/性能/故障注入）+ 15 项量化指标 |

---

## 四项核心原则（贯穿所有文档）

| # | 原则 | 当前状态 | 目标 |
|---|------|----------|------|
| 1 | **删除默认可撤销** | ✓ 回收站系统完善 | 所有删除路径走 trash；永久删除需输入确认 |
| 2 | **批量操作可取消** | ✗ 串行无进度无取消 | 后端 CancellationToken + 前端进度条 |
| 3 | **路径不越权** | ✗ delete_file 无校验 | PathGuard 模块 + CSP + 权限收敛 |
| 4 | **Agent 不自动执行破坏性操作** | ✗ execute_command 无限制 | 白名单 + Agent 仅生成计划等用户确认 |

---

## 立即行动项（P0）

以下 4 项为**必须立即修复**的安全/可靠性问题：

### P0-1 · Shell 命令执行无限制
- **位置**：`src-tauri/src/commands.rs` L1317-1336 (`execute_command`)
- **风险**：任意代码执行
- **修复**：白名单化或移除，改用 tauri-plugin-shell spawn

### P0-2 · `delete_file` 无路径安全校验
- **位置**：`src-tauri/src/commands.rs` L259-273
- **风险**：可删除系统关键文件
- **修复**：新建 `path_guard.rs`，所有文件操作入口统一调用

### P0-3 · CSP 禁用 + assetProtocol scope `["**"]`
- **位置**：`src-tauri/tauri.conf.json`
- **风险**：WebView 可加载任意资源，构成完整攻击链
- **修复**：启用严格 CSP，scope 限制为 `$HOME/**`

### P0-4 · 索引器相对路径持久化
- **位置**：`src-tauri/src/indexer.rs` L10
- **风险**：索引写入不可预期位置，不同启动方式数据不一致
- **修复**：改为 `app_data_dir/index`

---

## 分阶段能力规划

```
Phase 1 (1周)     安全加固 — 堵住攻击面
    ↓
Phase 2 (1.5周)   可靠性 — 原子写入 + 批量操作可取消
    ↓
Phase 3 (2周)     性能 — 流式IO + 分页 + 算法替换
    ↓
Phase 4 (2周)     架构 — App.tsx拆分 + 状态重组
    ↓
Phase 5 (2周)     产品增强 — Undo/QuickLook/搜索/Agent
```

**总周期**：约 8.5 周（1 人全职）或 5 周（2 人并行 Phase 3+4）

---

## 关键数字

| 维度 | 数据 |
|------|------|
| 已确认缺陷 | 15 项（P0×4, P1×8, P2×3）|
| 待验证假设 | 5 项 |
| 已存在能力（不重复建设） | 14 项 |
| 实施任务 | 40 项（5 阶段） |
| 量化验收指标 | 15 项 |
| 测试用例（规划） | 49 项（UT15 + IT8 + E2E6 + SEC7 + PERF8 + FI5） |
| 风险项 | 8 项 |
| 新增模块（规划） | path_guard.rs, atomic_write.rs, operation.rs, FileAgent.ts, undoStore.ts, useOperation.ts, QuickLook.tsx |

---

## 首要决策

1. **安全优先**：Phase 1 必须在任何功能开发前完成，否则存在用户数据风险
2. **渐进式重构**：不做大爆炸重写，新旧命令并存，前端逐步迁移
3. **流式 IO 为默认**：所有文件操作改为分块处理，恒定内存
4. **Agent 只建议不执行**：AI 生成的操作计划必须经用户确认
5. **测试跟随实现**：每个 Phase 的任务必须同步编写对应测试

---

*详细审计*：[01_现状审计与问题清单](./01_现状审计与问题清单.md)
